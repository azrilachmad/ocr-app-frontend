# =============================================================================
# Frontend Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values

# API Base URL - Backend server URL with /api suffix
# Development: http://localhost:3001/api
# Production: https://yourdomain.com/api (if using Nginx proxy)
#             or https://api.yourdomain.com/api (if using separate API domain)
VITE_API_BASE_URL=http://localhost:3001/api
